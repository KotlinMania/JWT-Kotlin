# Port report — `JWT-Kotlin`

> Generated by `scaffold/generate_repo_reports.py` from `workspace_dep_graph.json` ⨯ `port_priority.json`. Do not hand-edit. Re-run after the workspace dep graph is rebuilt.

## Overview

- This repo has **no `tmp/` Rust source** in the workspace. It's listed via manual override (`scaffold/build_workspace_dep_graph.py: PACKAGE_REPO_OVERRIDES`) because it hosts a logical port for `jsonwebtoken`.
- **Codex-kotlin distance**: **hop 1** from codex-kotlin via cross-repo Rust deps
- **Inbound**: 16 `use` sites from 2 other repos
- **Outbound**: 0 `use` sites to 0 other repos
- **Unresolved external**: 0 published Cargo crates with no `*-kotlin/` port

## Inbound — what to port first

Every symbol this repo's crates need to expose, ranked by total workspace demand. **Codex-kotlin sites are listed first per symbol** because codex is the priority consumer driving the port queue. Anything not listed here is not currently `use`'d by any kotlinmania repo and can be deferred.

### Inbound caller summary (16 edges, 3 caller crates)

| caller repo | caller crate | use sites | codex consumer? |
|---|---|---:|---|
| `codex-kotlin` | `codex-agent-identity` | 8 | ✅ |
| `codex-kotlin` | `codex-app-server-transport` | 4 | ✅ |
| `libwebrtc-kotlin` | `livekit-api` | 4 |  |

### Symbol-level demand (9 distinct symbols)

Sorted by codex demand, then total demand. Symbols with codex-kotlin sites are the priority queue. Each symbol entry below shows up to 12 call sites (codex first).

| symbol | total | codex | other | distinct callers |
|---|---:|---:|---:|---:|
| `jsonwebtoken::DecodingKey` | 3 | 2 | 1 | 3 |
| `jsonwebtoken::Algorithm` | 2 | 2 | 0 | 2 |
| `jsonwebtoken::Validation` | 2 | 2 | 0 | 2 |
| `jsonwebtoken::decode` | 2 | 2 | 0 | 2 |
| `jsonwebtoken::EncodingKey` | 2 | 1 | 1 | 2 |
| `jsonwebtoken::Header` | 2 | 1 | 1 | 2 |
| `jsonwebtoken::decode_header` | 1 | 1 | 0 | 1 |
| `jsonwebtoken::jwk::JwkSet` | 1 | 1 | 0 | 1 |
| `jsonwebtoken` | 1 | 0 | 1 | 1 |

#### Per-symbol detail (top 9 symbols)

##### `jsonwebtoken::DecodingKey` (3 sites, 2 codex, 1 other, 3 caller crates)

Codex call sites:

- `codex-kotlin/codex-agent-identity` `src/lib.rs:20` — `use jsonwebtoken::DecodingKey`
- `codex-kotlin/codex-app-server-transport` `src/transport/auth.rs:10` — `use jsonwebtoken::DecodingKey`

Other workspace call sites:

- `libwebrtc-kotlin/livekit-api` `src/access_token.rs:22` — `use jsonwebtoken::DecodingKey`

##### `jsonwebtoken::Algorithm` (2 sites, 2 codex, 0 other, 2 caller crates)

Codex call sites:

- `codex-kotlin/codex-agent-identity` `src/lib.rs:19` — `use jsonwebtoken::Algorithm`
- `codex-kotlin/codex-app-server-transport` `src/transport/auth.rs:9` — `use jsonwebtoken::Algorithm`

##### `jsonwebtoken::Validation` (2 sites, 2 codex, 0 other, 2 caller crates)

Codex call sites:

- `codex-kotlin/codex-agent-identity` `src/lib.rs:21` — `use jsonwebtoken::Validation`
- `codex-kotlin/codex-app-server-transport` `src/transport/auth.rs:11` — `use jsonwebtoken::Validation`

##### `jsonwebtoken::decode` (2 sites, 2 codex, 0 other, 2 caller crates)

Codex call sites:

- `codex-kotlin/codex-agent-identity` `src/lib.rs:22` — `use jsonwebtoken::decode`
- `codex-kotlin/codex-app-server-transport` `src/transport/auth.rs:12` — `use jsonwebtoken::decode`

##### `jsonwebtoken::EncodingKey` (2 sites, 1 codex, 1 other, 2 caller crates)

Codex call sites:

- `codex-kotlin/codex-agent-identity` `src/lib.rs:407` — `use jsonwebtoken::EncodingKey`

Other workspace call sites:

- `libwebrtc-kotlin/livekit-api` `src/access_token.rs:22` — `use jsonwebtoken::EncodingKey`

##### `jsonwebtoken::Header` (2 sites, 1 codex, 1 other, 2 caller crates)

Codex call sites:

- `codex-kotlin/codex-agent-identity` `src/lib.rs:408` — `use jsonwebtoken::Header`

Other workspace call sites:

- `libwebrtc-kotlin/livekit-api` `src/access_token.rs:22` — `use jsonwebtoken::Header`

##### `jsonwebtoken::decode_header` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-agent-identity` `src/lib.rs:23` — `use jsonwebtoken::decode_header`

##### `jsonwebtoken::jwk::JwkSet` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-agent-identity` `src/lib.rs:24` — `use jsonwebtoken::jwk::JwkSet`

##### `jsonwebtoken` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `libwebrtc-kotlin/livekit-api` `src/access_token.rs:22` — `use jsonwebtoken`

## Outbound — what this repo depends on

Other kotlinmania repos this repo's Rust source `use`s. Symbol-level detail per target lives in **that target repo's PORT_REPORT.md inbound section** (no duplication). Sorted by Jira rank, then by `use`-site count.

_No outbound edges to other workspace repos._

## Unresolved external dependencies

Published Cargo crates this repo's Rust source `use`s but for which no `<crate>-kotlin/` repo exists. Each is either a missing port to file in Jira, or an intentional external dep (system library / FFI / build-only). If a port lives at a non-conventional repo name, add it to `PACKAGE_REPO_OVERRIDES` in `scaffold/build_workspace_dep_graph.py`.

_None — every external dep has a `*-kotlin/` port._
